BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[com] (
    [co] INT NOT NULL IDENTITY(1,1),
    [com] VARCHAR(100) NOT NULL,
    CONSTRAINT [com_pkey] PRIMARY KEY CLUSTERED ([co])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

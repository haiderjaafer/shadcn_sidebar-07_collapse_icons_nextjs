
export type UserPayload = {
    userName: string;
    employeeHireDate: string; // Dates are typically sent as strings in JSON
    comcommittee: number;
    department: number;
    unit: number;
  };

